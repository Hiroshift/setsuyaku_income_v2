<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover">
    <meta name="description" content="節約を収入として可視化するモバイルアプリ">
    <meta name="theme-color" content="#f6f5f0">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="apple-mobile-web-app-title" content="節約は収入">
    <title>節約は収入</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- ヘッダー -->
    <header>
      <div class="header-inner">
        <h1 class="logo">
          <a href="<%= root_path %>"><span class="logo-mark">¥</span> 節約は収入</a>
        </h1>
        <nav class="nav-actions">
          <% if user_signed_in? %>
            <span class="nav-user"><%= current_user.nickname %></span>
            <% unless controller_name == "registrations" && action_name == "edit" %>
              <%= link_to "設定", edit_user_registration_path, class: "nav-pill nav-pill--ghost" %>
            <% end %>
            <%= link_to "OUT", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-pill nav-pill--muted" %>
          <% else %>
            <% if controller_name == "registrations" && action_name == "new" %>
              <%= link_to "ログイン", new_user_session_path, class: "nav-pill nav-pill--ghost" %>
            <% elsif controller_name == "sessions" && action_name == "new" %>
              <%= link_to "はじめる", new_user_registration_path, class: "nav-pill nav-pill--gold" %>
            <% else %>
              <%= link_to "はじめる", new_user_registration_path, class: "nav-pill nav-pill--gold" %>
              <%= link_to "ログイン", new_user_session_path, class: "nav-pill nav-pill--ghost" %>
            <% end %>
          <% end %>
        </nav>
      </div>
    </header>

    <!-- ページ -->
    <div class="page">
      <main class="page-body">
        <% flash.each do |key, message| %>
          <div class="flash <%= key == 'notice' ? 'flash--success' : 'flash--error' %>">
            <%= message %>
          </div>
        <% end %>

        <%= yield %>
      </main>

      <footer class="page-footer">
        &copy; <%= Date.today.year %> 節約は収入
      </footer>
    </div>
  </body>
</html>
